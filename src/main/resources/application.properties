spring.application.name=applist-backend
spring.docker.compose.enabled=false
server.port=8080
info.app.version=@git.tags@

# 배포 관리자 계정 (운영환경에서는 application-prod.properties 에서 오버라이드)
app.admin.username=rusty-deploy
app.admin.password=zja2592zja!@#

# 기본 SSH 키 경로 (배포설정에서 개별 지정 시 개별 값 우선)
app.deploy.default-key-path=C:/rsa/id_rsa_deploy